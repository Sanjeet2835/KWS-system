numpy>=1.24
scipy>=1.10
sounddevice>=0.4.6
pygame>=2.5
pyserial>=3.5
# Optional on the Pi for I2S capture:
# pyalsaaudio
# Optional ASR (server). Prefer faster-whisper (Hugging Face base.en) on the Pi.
# vosk
# faster-whisper
# Optional INT8 runtime on the Pi (lighter than full TensorFlow):
# tflite-runtime
